# ── App ────────────────────────────────────────────────────────
# Public site origin, used as a fallback for auth email redirect links
# when the request Origin header is unavailable. e.g. http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Supabase (M1) ──────────────────────────────────────────────
# Project: affiliateos-prod (eu-central-1). Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role: server / edge functions only. NEVER expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# ── Anthropic (M3, stubbed in M1) ──────────────────────────────
ANTHROPIC_API_KEY=

# ── Langfuse (M1, AI tracing) ──────────────────────────────────
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# ── Sentry (M1, error tracking) ────────────────────────────────
NEXT_PUBLIC_SENTRY_DSN=

# ── Resend (M2+, transactional email) ──────────────────────────
RESEND_API_KEY=

# ── Cron (M3+, eval regression) ────────────────────────────────
# Shared secret used to authenticate Vercel Cron requests.
CRON_SECRET=

# ── Stripe (M5, billing) ───────────────────────────────────────
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# ── PostHog (M5, product analytics) ────────────────────────────
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ── Discovery Scanner (web search) ─────────────────────────────
# Tavily by default; mock fallback when unset. Add as a Supabase secret
# (edge fn reads it), not Vercel.
DISCOVERY_SEARCH_API_KEY=
DISCOVERY_SEARCH_PROVIDER=tavily
